import localFont from 'next/font/local';

/** Samim (OFL, see app/fonts/Samim-LICENSE.txt): an optional headline font for landing pages. */
export const samim = localFont({ src: '../../../app/fonts/Samim.ttf', variable: '--font-samim', display: 'swap', preload: false });
